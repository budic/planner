%h1 Listing schedule_blocks

%table.table.table-condensed
  %tr
    %th Schedule
    %th Schedule Block
    %th
    %th
    %th

  - @schedule_blocks.each do |schedule_block|
    %tr
      %td= schedule_block.schedule.today
      %td= schedule_block.id
      %td= link_to 'Show', schedule_block
      %td= link_to 'Edit', edit_schedule_block_path(schedule_block)
      %td= link_to 'Destroy', schedule_block, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Schedule block', new_schedule_block_path
