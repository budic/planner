%h1 Listing todos

%table
  %tr
    %th Schedule
    %th Task
    %th Position
    %th Description
    %th
    %th
    %th

  - @todos.each do |todo|
    %tr
      %td= todo.schedule_id
      %td= todo.task_id
      %td= todo.position
      %td= todo.try(:task).try(:task)
      %td= link_to 'Show', todo
      %td= link_to 'Edit', edit_todo_path(todo)
      %td= link_to 'Destroy', todo, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Todo', new_todo_path
